# Scripts

This directory will contain utility and migration scripts for the Siege Assignment System.

Planned scripts:
- `seed_db.py` — populate the database with initial/test data
- `run_migrations.sh` — wrapper for running Alembic migrations in CI/CD
- `import_members.py` — bulk import of guild members from Discord

Scripts will be added as needed throughout development phases.
